
function tot_oscars(n_of_oscars){

    if (n_of_oscars.value == 1){
      document.write("That's right buddy!! He only won" + n_of_oscars.value) 
    } else {
      document.write("Not quite the right answer... try again!")
    }
}
